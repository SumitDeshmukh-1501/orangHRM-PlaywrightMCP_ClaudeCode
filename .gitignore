# Dependencies
node_modules/

# Environment variables — never commit real secrets/config
.env

# Playwright runtime artifacts
test-artifacts/
reports/
playwright-report/
blob-report/
playwright/.cache/

# Saved authenticated browser session written by tests/ui/auth.setup.ts.
# This is a credential artifact (it contains a live session cookie) — never commit it.
playwright/.auth/

# Build output (if any is ever added)
dist/
build/

# Editor/OS noise
.vscode/*
!.vscode/extensions.json
.DS_Store
Thumbs.db

# Logs
*.log
npm-debug.log*